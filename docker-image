If your system has a global Artifactory configured, run:

pip install <package> --no-index --find-links https://pypi.org/simple


But the simpler version is:

pip install <package> --index-url https://pypi.org/simple --no-extra-index-url


This overrides everything else.